/* Konfiguracja synchronizacji.
   Zostaw puste, a aplikacja działa wyłącznie lokalnie, bez kont i bez sieci.
   Po założeniu projektu w Supabase wklej tu dwie wartości z zakładki
   Project Settings → API. Klucz "anon" jest publiczny i może tu być. */
window.KOCIA_CONFIG = {
  SUPABASE_URL: "",
  SUPABASE_ANON_KEY: ""
};
